<?xml version="1.0"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.mixxx"
    android:installLocation="auto"
    android:versionCode="1"
    android:versionName="1.0">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"  />
    <uses-permission android:name="android.permission.INTERNET"  />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"  />
    <uses-permission android:name="android.permission.RECORD_AUDIO"  />
    <uses-permission android:name="android.permission.USB_PERMISSION"/>
    <!-- BLE MIDI: BLUETOOTH_CONNECT is a runtime permission on API 31+, requested
         via Qt's QPermissions API (QBluetoothPermission) from the controllers
         preferences page. The legacy BLUETOOTH/BLUETOOTH_ADMIN permissions cover
         Android 11 and older, where they're install-time. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <!-- BLE MIDI device discovery; neverForLocation because we only match
         the MIDI service UUID, not derive location. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
            android:usesPermissionFlags="neverForLocation"/>
    <!-- Not used for advertising - declared anyway because Qt's
         QBluetoothPermission checks for this alongside SCAN/CONNECT on
         API 31+ and reports Denied outright (not Undetermined) for the
         whole permission if it's undeclared, even when SCAN/CONNECT are
         both actually granted. -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"/>
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"  />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true" />
    <application
        android:name="org.qtproject.qt.android.bindings.QtApplication"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Mixxx"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:fullBackupOnly="false">
        <activity
            android:name="org.mixxx.MainActivity"
            android:configChanges="orientation|uiMode|screenLayout|screenSize|smallestScreenSize|layoutDirection|locale|fontScale|keyboard|keyboardHidden|navigation|mcc|mnc|density"
            android:launchMode="singleTop"
            android:screenOrientation="sensorLandscape"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

          <meta-data
                android:name="android.app.lib_name"
                android:value="mixxx" />

          <meta-data
                android:name="android.app.background_running"
                android:value="true" />

          <meta-data
                android:name="android.app.splash_screen_drawable"
                android:resource="@drawable/ic_launcher_foreground" />

          <meta-data
                android:name="android.app.arguments"
                android:value="--qml --log-level debug --developer" />
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.qtprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/qtprovider_paths"/>
        </provider>
    </application>

    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="true" />
    <!-- Not required=true: MIDI controller support (AndroidMidiController)
         should degrade gracefully rather than block install on the
         (increasingly rare) Android device without it. -->
    <uses-feature
        android:name="android.hardware.midi"
        android:required="false" />
</manifest>
